# 🤖 YODA.RAG — Анализ GitHub-репозиториев через ИИ

Этот инструмент позволяет **задавать вопросы по коду любого публичного репозитория на GitHub** — и получать точные ответы с указанием файлов и фрагментов кода.

Под капотом:  
- Клонирование репозитория  
- Индексация кода с помощью эмбеддингов  
- Поиск релевантных фрагментов  
- Ответ от локальной языковой модели (LLM)

---

## 🚀 Как запустить

### Требования
- Python 3.9+
- [Ollama](https://ollama.com/) или другой LLM-сервер на `http://127.0.0.1:1234/v1`
- Git

### Установка
```bash
# Клонируйте репозиторий
git clone ваш-репозиторий
cd ваш-репозиторий

# Установите зависимости
pip install -r requirements.txt
gitpython==3.1.40
langchain==0.1.5
gradio==4.13.0
langfuse==2.0.0
requests==2.31.0
numpy==1.26.0
sentence-transformers==2.2.2
scikit-learn==1.3.0
tqdm==4.65.0
torch>=2.0.0
transformers>=4.30.0
это все что надо
Запуск
python app.py


💡 Как пользоваться
Вставьте URL публичного репозитория на GitHub
Пример: https://github.com/pytorch/pytorch
Задайте вопрос:
«Как работает система логирования?»
«Где определён класс DataLoader?»
«Как инициализируется модель?»
Получите ответ с указанием:
Файлов
Конкретных строк кода
Уровня релевантности
Настройки
В интерфейсе можно регулировать:

Temperature — креативность ответа
Max Tokens — длина ответа
Top P / Top K — контроль над выбором токенов и контекста
🛠 Что внутри
VectorStore — хранит эмбеддинги файлов и чанков
GitRAGSystem — клонирует репо, парсит код, генерирует ответы
Gradio — дружелюбный веб-интерфейс
Поддержка любых LLM через OpenAI-совместимый API
📝 Заметки
Репозитории кэшируются локально в папке ./temp_repos
Пароли и приватные репо не поддерживаются
Работает только с публичными репозиториями
Сделано для разработчиков, которые хотят быстро понять чужой код — без чтения тысяч строк вручную.
